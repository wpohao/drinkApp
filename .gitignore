# === Xcode / Swift 專案 .gitignore ===
# 目的：把「編譯產物、使用者個人狀態、相依套件快取」擋在 git 外，
# 讓 repo 只留原始碼，維持個位數 MB，免費 GitHub 額度即足夠。

## macOS
.DS_Store

## Xcode 使用者個人設定 / 狀態
xcuserdata/
*.xcuserstate
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.moved-aside
*.xccheckout
*.xcscmblueprint

## 編譯產物（最會撐大 repo，一定要排除）
build/
DerivedData/
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

## Swift Package Manager
.build/
.swiftpm/
# 註：Package.resolved 建議「保留 commit」以鎖定套件版本，故不忽略。

## CocoaPods（若日後改用，取消下一行註解）
# Pods/

## Carthage
Carthage/Build/

## fastlane（若日後導入自動化上架）
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

## 機密：API 金鑰、憑證、AdMob/Firebase 設定（避免外洩，勿進 repo）
*.mobileprovision
*.p12
*.p8
# GoogleService-Info.plist
Secrets.xcconfig
