# === Android / Gradle 專案 .gitignore ===
# 目的：把「編譯產物、使用者個人設定、機密金鑰」擋在 git 外，
# 讓 repo 只留原始碼，維持個位數 MB，免費 GitHub 額度即足夠。

## 作業系統
.DS_Store
Thumbs.db

## Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
# 註：gradle-wrapper.jar 要保留 commit，讓 clone 者不必先裝 Gradle。

## 編譯產物（最會撐大 repo，一定要排除）
*.apk
*.aab
*.ap_
*.dex
*.class
release/
captures/

## Android Studio / IntelliJ 使用者個人設定
.idea/
*.iml
.navigation/
misc/

## 本機專案設定（含 SDK 路徑，因機而異，不進 repo）
local.properties

## NDK / 原生
obj/
.cxx/

## 測試與覆蓋率報告
/reports/
lint-results*.xml

## 機密：簽章金鑰、AdMob/Firebase 設定、API 金鑰（避免外洩，勿進 repo）
*.jks
*.keystore
keystore.properties
signing.properties
# google-services.json   # 若導入 Firebase，內含專案設定，視情況取消註解排除
secrets.properties
*.p12
